{
 "proxies": {
   "default": {
     "httpProxy": "{{ proxy_url }}",
     "httpsProxy": "{{ proxy_url }}",
     "noProxy": "{{ no_proxy | default('127.0.0.1,::1,localhost,10.0.0.0/8,192.168.0.0/16') }}"
   }
 }
}
